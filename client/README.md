# Spotify Rooms Frontend

React application for Spotify collaborative playback.

## Quick Start

1. Install dependencies: `npm install`
2. Start development server: `npm start`
3. Build for production: `npm run build`

See `DOC.md` in project root for complete setup instructions.
