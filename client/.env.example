# API Configuration
# Leave empty if using proxy (development)
# Set to backend URL in production (e.g., https://api.yourapp.com)
REACT_APP_API_URL=

# WebSocket URL
# Leave empty to auto-detect from current location
# Set explicitly in production if needed (e.g., wss://api.yourapp.com)
REACT_APP_WS_URL=
