# Spotify Rooms Backend

Server for Spotify collaborative playback rooms with PostgreSQL persistence.

## Quick Start

1. Install dependencies: `npm install`
2. Copy `.env.example` to `.env` and configure
3. Initialize database: Run `schema.sql` in PostgreSQL
4. Start server: `npm start`

See `DOC.md` in project root for complete setup instructions.
